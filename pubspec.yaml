name: aerofind
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  lottie: ^2.6.0
  google_fonts: ^6.2.1
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.12.3  # latest version
  geocoding: ^2.0.0  # convert address to lat/lng
  google_places_flutter: ^2.1.0
  permission_handler: ^12.0.1
  location: ^8.0.1
  http: ^1.4.0
  shared_preferences: ^2.5.3
  image_picker: ^1.1.2
  http_parser: ^4.0.2
  image: ^4.1.7   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
